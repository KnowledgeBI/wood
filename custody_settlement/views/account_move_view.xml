<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <record id="view_account_invoice_filter" model="ir.ui.view">
        <field name="name">account.move.form</field>
        <field name="model">account.move</field>
        <field name="mode">primary</field>
        <field name="inherit_id" ref="account.view_account_invoice_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='salesperson']" position="before">
                <filter string="Extra Approvals" name="approval_levels" context="{'group_by':'approval_levels'}"/>
            </xpath>
            <xpath expr="//filter[@name='cancel']" position="before">
                <filter name="waiting_approve" string="Waiting Approve" domain="[('approval_levels', '=', 'waiting')]"/>
                <filter name="approved" string="Approved" domain="[('approval_levels', '=', 'approved')]"/>

            </xpath>
        </field>
    </record>
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.form</field>
        <field name="model">account.move</field>
        <field name="mode">primary</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@class='o_td_label']" position="replace">
                <div class="o_td_label">
                    <label for="partner_id" string="Employee" style="font-weight:bold;"
                           attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt'))]}"/>
                    <label for="partner_id" string="Employee" style="font-weight:bold;"
                           attrs="{'invisible': [('move_type', 'not in', ('in_invoice', 'in_refund', 'in_receipt'))]}"/>
                </div>
            </xpath>
            <xpath expr="//button[@name='action_post']" position="attributes">
                <attribute name="groups">custody_settlement.group_custody_manager</attribute>
            </xpath>
            <xpath expr="//button[@name='action_post'][2]" position="attributes">
                <attribute name="groups">custody_settlement.group_custody_manager</attribute>
            </xpath>

            <xpath expr="//button[@name='action_post']" position="after">
                <button name="request_approve" string="Request Approve"
                        groups="custody_settlement.group_custody_manager,custody_settlement.group_custody_user"
                        class="oe_highlight" type="object" attrs="{'invisible': [('approval_levels', '!=', 'none')]}"/>

            </xpath>
            <xpath expr="//field[@name='journal_id']" position="after">
                <field name="approval_levels" readonly="1" invisible="1"/>
            </xpath>

            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="partner_balance" readonly="1" force_save="1"/>
                <field name="estimated_balance" readonly="1" force_save="1"/>
                <field name="employee_contacts" widget="many2many_tags" invisible="1" force_save="1"/>
                <field name="analytic_tags_ids" widget="many2many_tags" groups="analytic.group_analytic_tags"
                       attrs="{'readonly':[('state','not in',('draft','sent'))],'required':[('move_type','=','in_invoice'),('custody_settlement','=',True)]}"/>
                <field name="custody_settlement" invisible="1" force_save="1"/>
            </xpath>
            <xpath expr="//field[@name='invoice_line_ids']/tree//field[@name='analytic_tag_ids']" position="attributes">
                <attribute name="required">True</attribute>
            </xpath>
            <xpath expr="//field[@name='invoice_line_ids']/tree//field[@name='name']" position="after">
                <field name="partner_id"/>
                <field name="attachment_file" widget="binary" filename="attachment_name"/>
                <field name="attachment_name" invisible="1"/>
            </xpath>
            <xpath expr="//field[@name='line_ids']/tree//field[@name='name']" position="after">
                <field name="attachment_file" invisible="1" widget="binary" filename="attachment_name"/>
                <field name="attachment_name" invisible="1"/>
            </xpath>
            <xpath expr="//group[@id='header_left_group']/field[@name='partner_id']" position="replace">
                <field name="partner_id" domain="[('id','in',employee_contacts)]" widget="res_partner_many2one"
                       nolabel="1"
                       context="{
                                            'res_partner_search_mode': (context.get('default_move_type', 'entry') in ('out_invoice', 'out_refund', 'out_receipt') and 'customer') or (context.get('default_move_type', 'entry') in ('in_invoice', 'in_refund', 'in_receipt') and 'supplier') or False,
                                            'show_address': 1, 'default_is_company': True, 'show_vat': True}"
                       options='{"always_reload": True, "no_quick_create": True}'
                       attrs="{'invisible': [('move_type', 'not in', ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt'))]}"/>

            </xpath>


        </field>
    </record>

</odoo>
